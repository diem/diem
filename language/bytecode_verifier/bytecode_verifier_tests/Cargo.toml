[package]
name = "libra_bytecode_verifier_tests"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
publish = false
edition = "2018"

[dev-dependencies]
petgraph = "0.4"
proptest = "0.9.2"
libra_bytecode_verifier = {path = "../", features = ["testing"]}
failure = { path = "../../../common/failure_ext", package = "libra_failure_ext" }
libra_types = { path = "../../../types", features = ["testing"]}
libra_invalid_mutations = { path = "../invalid_mutations" }
libra_vm = { path = "../../vm", features = ["testing"]}
