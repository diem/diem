[package]
name = "libra_transaction_builder"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
edition = "2018"

[dependencies]
libra_config = { path = "../../config" }
libra_crypto = { path = "../../crypto/crypto" }
libra_ir_to_bytecode = { path = "../compiler/ir_to_bytecode" }
lazy_static = "1.3.0"
libra_stdlib = { path = "../stdlib" }
libra_types = { path = "../../types" }
libra_vm = { path = "../vm" }

[dev-dependencies]
libra_types = { path = "../../types", features = ["testing"]}

[features]
testing = ["libra_types/testing", "libra_crypto/testing"]
