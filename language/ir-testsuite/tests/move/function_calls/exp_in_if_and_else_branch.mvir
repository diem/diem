main() {
    if (true) {
        (4);
        // check: POSITIVE_STACK_SIZE_AT_BLOCK_END
    }
    else {
        (5);
        // POSITIVE_STACK_SIZE_AT_BLOCK_END, but we only check one error
    }
    return;
}
