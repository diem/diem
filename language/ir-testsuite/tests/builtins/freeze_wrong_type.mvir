main() {
    let x: u64;
    let x_ref: &mut u64;
    let imm_ref: &bool;

    x = 5;
    x_ref = &mut x;
    imm_ref = freeze(move(x_ref));
    _ = move(imm_ref);
    return;
}

// check: STLOC_TYPE_MISMATCH_ERROR
