main() {
    let x: u64;
    let y: u64;

    x = 0;
    if (true) {
        y = 0;
    } else {
        y = move(x);
    }
    assert(copy(x) == 0, 42);
    return;
}

// check: COPYLOC_UNAVAILABLE_ERROR
