// (u64, u64) args to main(u64, address) is an error

//! args: 42, 42

main(x: u64, y: address) {
    return;
}

// TODO(status_migration) breaks if Keep is first
// check: TYPE_MISMATCH
// check: Keep
