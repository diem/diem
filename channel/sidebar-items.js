initSidebarItems({"fn":[["new","Similar to `mpsc::channel`, `new` creates a pair of `Sender` and `Receiver`"],["new_test",""]],"mod":[["diem_channel","diem_channel provides an mpsc channel which has two ends `diem_channel::Receiver` and `diem_channel::Sender` similar to existing mpsc data structures. What makes it different from existing mpsc channels is that we have full control over how the internal queueing in the channel happens and how we schedule messages to be sent out from this channel. Internally, it uses the `PerKeyQueue` to store messages"],["message_queues",""]],"struct":[["Receiver","An `mpsc::Receiver` with an [`IntGauge`] counting the number of currently queued items."],["Sender","An `mpsc::Sender` with an [`IntGauge`] counting the number of currently queued items."]]});