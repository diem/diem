[package]
name = "libra-error-with-context"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
description = "Libra error that holds an error context"
repository = "https://github.com/libra/libra"
homepage = "https://libra.org"
license = "Apache-2.0"
publish = false
edition = "2018"

[dependencies]
backtrace = "0.3"

libra-workspace-hack = { path = "../../common/workspace-hack", version = "0.1.0" }

[dev-dependencies]
thiserror = "1.0.20"
