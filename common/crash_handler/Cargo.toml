[package]
name = "crash_handler"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
publish = false
edition = "2018"

[dependencies]
backtrace = "0.3.9"
toml = "0.4.7"

logger = { path = "../logger" }
serde = { version = "1.0.89", features = ["derive"] }
