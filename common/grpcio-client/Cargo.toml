[package]
name = "libra_grpcio-client"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
publish = false
edition = "2018"

[dependencies]
protobuf = "~2.7"
protobuf-codegen = "2.*"
protoc = "2.*"
protoc-grpcio = "1.0.2"
regex = { version = "1.3.0", default-features = false, features = ["std", "perf"] }

libra_tools = { path = "../tools/"}
