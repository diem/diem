[package]
name = "libra_executable_helpers"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
publish = false
edition = "2018"

[dependencies]
clap = "2.32.0"
slog-scope = "4.0"

libra_config = { path = "../../config"}
libra_crash_handler = { path = "../crash_handler" }
libra_logger =  { path = "../logger" }
libra_metrics = { path = "../metrics" }
