[package]
name = "libra_debug_interface"
version = "0.1.0"
authors = ["Libra Association <opensource@libra.org>"]
license = "Apache-2.0"
publish = false
edition = "2018"

[dependencies]
grpcio = { version = "0.4.4", default-features = false, features = ["protobuf-codec"] }
futures = "0.1.28"
protobuf = "~2.7"
serde_json = "1.0"
lazy_static = "1.3.0"

failure = { package = "libra_failure_ext", path = "../failure_ext" }
libra_logger = { path = "../logger" }
libra_metrics = { path = "../metrics" }

[build-dependencies]
libra_build_helpers = { path = "../build_helpers" }
