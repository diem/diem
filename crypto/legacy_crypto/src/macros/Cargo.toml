[package]
name = "crypto-derive"
version = "0.1.0"
description = "Custom derives for `crypto`"
edition = "2018"

[lib]
proc_macro = true

[dependencies]
syn = { version = "1.0.1", features = ["derive"] }
quote = "1.0.0"
