[package]
name = "libra_crypto_derive"
version = "0.1.0"
description = "Custom derives for `crypto`"
edition = "2018"

[lib]
proc_macro = true

[dependencies]
syn = { version = "1.0.1", features = ["derive"] }
quote = "1.0.0"
proc-macro2 = "1.0.1"

[dev-dependencies]
libra_crypto = { path = "../crypto"}
failure = { path = "../../common/failure_ext", package = "libra_failure_ext"}
