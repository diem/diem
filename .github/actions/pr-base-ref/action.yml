name: pr base ref
description: Get the base ref for a pull request
inputs:
  github-token:
    description: GitHub token
    required: true
outputs:
  ref:
    description: the base ref of the pull request
runs:
  using: 'node12'
  main: 'dist/index.js'
