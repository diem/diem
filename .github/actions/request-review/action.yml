name: Request review
description: Request review on a PR
inputs:
  users:
    description: user list (comma separated)
    required: false
  teams:
    description: team list (comma separated)
    required: false
runs:
  using: 'node12'
  main: 'dist/index.js'
